{
  "name": "allow",
  "version": "1.0.0",
  "description": "allows users to add port 2222 with their desired ip address to kube-host's security group and removes it after 15 minutes",
  "main": "allow.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1454.0",
    "express": "^4.18.2"
  }
}
